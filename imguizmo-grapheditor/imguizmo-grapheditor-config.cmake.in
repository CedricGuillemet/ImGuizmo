if (NOT TARGET guizmo)
    include(${CMAKE_CURRENT_LIST_DIR}/grapheditor-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/grapheditor-targets.cmake)
endif()

check_required_components(imgui-core)