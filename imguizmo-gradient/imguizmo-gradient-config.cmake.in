if (NOT TARGET guizmo)
    include(${CMAKE_CURRENT_LIST_DIR}/gradient-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/gradient-targets.cmake)
endif()

check_required_components(imgui-core)