if (NOT TARGET guizmo)
    include(${CMAKE_CURRENT_LIST_DIR}/curveedit-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/curveedit-targets.cmake)
endif()

check_required_components(imgui-core)