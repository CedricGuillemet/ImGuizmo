if (NOT TARGET guizmo)
    include(${CMAKE_CURRENT_LIST_DIR}/zoomslider-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/zoomslider-targets.cmake)
endif()

check_required_components(imgui-core)