if (NOT TARGET guizmo)
    include(${CMAKE_CURRENT_LIST_DIR}/sequencer-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/sequencer-targets.cmake)
endif()

check_required_components(imgui-core)